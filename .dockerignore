# Build outputs and dependencies are produced inside the image.
node_modules/
dist/
.ui5/

# Source documents are inputs to the seed generator, not to the runtime.
# The generated migration is committed, so the image does not need the
# workbook (which is also the largest thing in the repo).
docs/

# Version control and tooling
.git/
.github/
.gitignore

# Local environment
.env
.env.local

# Editors and OS
.DS_Store
.idea/
.vscode/
*.swp
